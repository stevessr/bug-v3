export function initSaveLastDiscourse() {
  // disabled: previously injected a "保存为最近 Discourse" button into Discourse pages.
  // Keeping this no-op preserves API (no dynamic import failures) while removing UI injection.
  return
}
