import { createApp } from 'vue'

import ImageGeneratorMain from '@/components/ImageGenerator/ImageGeneratorMain.vue'

// Create and mount the Vue application
const app = createApp(ImageGeneratorMain)

app.mount('#app')
