// Image generator entry removed — minimal placeholder to reduce type-check noise.
// The full UI implementation was intentionally removed earlier.
export const IMAGE_GENERATOR_REMOVED = true

// Keep a tiny runtime-friendly placeholder for consumers that import this module.
export function isImageGeneratorAvailable(): boolean {
  return false
}
