{
  "name": "video2gif-static",
  "version": "1.0.0",
  "description": "Video to GIF converter - Static site for Cloudflare Pages",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2024-01-01",
    "deploy": "wrangler pages deploy public --project-name=video2gif-pages"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
