# Cloudflare Pages configuration
# This project is a static site; we serve files from the `public` directory.

name = "video2gif-pages"
pages_build_output_dir = "public"
compatibility_date = "2024-01-01"

# Remove worker-specific fields (main, usage_model, workers_dev)
# Functions can be added later under ./functions if needed.