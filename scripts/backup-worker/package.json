{
  "name": "emoji-backup-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for emoji extension backup with password protection.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
